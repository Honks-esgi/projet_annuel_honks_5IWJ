{
  "name": "honks",
  "version": "0.1.0",
  "private": true,
  "description": "Honks est un projet composé de trois produits principaux:\r - une API backend\r - une application web (site vitrine + administration)\r - une application mobile",
  "license": "ISC",
  "author": "GRONDIN Florent, SANKARE Jordan, VANDERLYNDEN Louis-Martin",
  "type": "commonjs",
  "directories": {
    "doc": "docs"
  },
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:dev": "docker compose -f docker-compose.dev.yml up -d --build",
    "docker:preprod": "docker compose -f docker-compose.preprod.yml up -d --build",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d --build"
  }
}
